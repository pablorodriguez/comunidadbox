- javascript 'accounts'
%h1
  = vineta
  Nuevo Empleado para  
  = current_user.company.name
- form_for @employee, :url => employees_path do |f|
  .box
    = render :partial => 'form',:locals  =>{:f => f}
    = f.submit "Grabar",:class =>"btn"
    = link_to_back employees_path

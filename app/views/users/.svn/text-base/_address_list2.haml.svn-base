%table
  %tr
    %th
      Tipo
    %th
      Calle
    %th
      Ciudad
    %th
      Código Postal
    %th
      Provincia
    %th
      Sucursal
  - if @show_emp_edit
    - @addresses = current_user.company.users.first.addresses
  - else
    - @addresses = current_user.addresses
  - @addresses.each do |address|
    %tr{:class=>:fields}
      %td
        =h address.name
      %td
        =h address.street
      %td
        =h address.city
      %td
        =h address.zip
      %td
        =h address.state.name
      %td
        = f.radio_button :branch, address.id


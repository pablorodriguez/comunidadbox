-# javascript 'accounts'
.vertical_menu
  = render "admin/menu"
  = render "actions"
.contentright_s
  .box_bottom
    %h1
      Modificar Configuración para
      = @company_attribute.company.name
  
  = form_for @company_attribute do |f|    
    .box_bottom
      = render :partial => 'form',:locals  =>{:f => f}
    .box_bottom.buttons_container
      = f.submit "Actualizar",:class =>"button"
      = link_to "Cancelar",current_user.company_attributes
.menu_actions
  .box_bottom
    %ul
      %li
        - if company_id
          = link_to t(".new_client"),new_client_path,{:title=>t(".create_new_client")}
        - else
          = link_to t("new_vehicle"), new_vehicle_path(:user_id=>current_user.id)
      = yield :actions
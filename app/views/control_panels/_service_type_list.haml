- if @company_services.size > 0
  %h1 Tipos de Servicios
  %ul
    -for service in @company_services
      %li
        = link_to service.name,filter_alarms_control_panels_path(:st => service)
-else
  = render 'shared/message', :msg =>"No ha configurado tipos de servicios"
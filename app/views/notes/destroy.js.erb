$("#note-<%=@note_id%>").fadeOut().remove();

if ($("<%=@element_id%> .notes").children().size() == 0){
  var notes_container = $("<%=@element_id%> .notes_container");
  if (!(notes_container.find(".note_form").is(":visible"))){
    notes_container.hide();  
  }

	$("<%=@element_id%>").find(".notes_link").parent().hide();
  
	
}
-# javascript 'service_offer'
-# javascript 'service_offer'
.vertical_menu
  = render "admin/menu"
  = render "actions"
.contentright_s
  .box_bottom
    %h1
      = t('.edit_status')
  = form_for @custom_status do |f|
    = render 'shared/error_messages', :target => @custom_status
    = render :partial => 'form', :locals => {:f => f}


%br
%table#sort_table.display
  %thead
    %tr
      %th Nombre
      %th
  - for service in @materials.service_types
    %tr
      %td 
        = service.name
      %td
        = link_to image_tag('delete.gif'), {:action => 'destroy_servicetype', :id => service.id}, :confirm => '¿Esta seguro que desea borrarlo?', :method => :delete

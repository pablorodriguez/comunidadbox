%table.comboxtable
  %thead
    %tr
      %th Nombre
      %th  
  %tbody
    - current_user.service_types.each do |service_type|
      %tr{:id => service_type.id}
        = render :partial => "service_type_row",:locals =>{material: material,:service_type_row => service_type}